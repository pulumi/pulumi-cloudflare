// *** WARNING: this file was generated by pulumi-language-java. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

package com.pulumi.cloudflare.emailSecurityBlock;

import com.pulumi.core.Output;
import com.pulumi.core.annotations.Import;
import com.pulumi.exceptions.MissingRequiredPropertyException;
import java.lang.Boolean;
import java.lang.String;
import java.util.Objects;
import java.util.Optional;
import javax.annotation.Nullable;


public final class SenderArgs extends com.pulumi.resources.ResourceArgs {

    public static final SenderArgs Empty = new SenderArgs();

    /**
     * Identifier.
     * 
     */
    @Import(name="accountId", required=true)
    private Output<String> accountId;

    /**
     * @return Identifier.
     * 
     */
    public Output<String> accountId() {
        return this.accountId;
    }

    @Import(name="comments")
    private @Nullable Output<String> comments;

    public Optional<Output<String>> comments() {
        return Optional.ofNullable(this.comments);
    }

    @Import(name="isRegex", required=true)
    private Output<Boolean> isRegex;

    public Output<Boolean> isRegex() {
        return this.isRegex;
    }

    /**
     * The pattern value to match against. Format depends on `patternType`:
     * - EMAIL: a valid email address, e.g. `user{@literal @}example.com`
     * - DOMAIN: a valid domain name, e.g. `example.com`
     * - IP: a plain IPv4 address (e.g. `1.2.3.4`) or an IPv4 CIDR block (e.g. `1.2.3.0/24`). Only globally reachable addresses are accepted; private, loopback, link-local, and unspecified addresses are rejected.
     * 
     */
    @Import(name="pattern", required=true)
    private Output<String> pattern;

    /**
     * @return The pattern value to match against. Format depends on `patternType`:
     * - EMAIL: a valid email address, e.g. `user{@literal @}example.com`
     * - DOMAIN: a valid domain name, e.g. `example.com`
     * - IP: a plain IPv4 address (e.g. `1.2.3.4`) or an IPv4 CIDR block (e.g. `1.2.3.0/24`). Only globally reachable addresses are accepted; private, loopback, link-local, and unspecified addresses are rejected.
     * 
     */
    public Output<String> pattern() {
        return this.pattern;
    }

    /**
     * Type of pattern matching.
     * - EMAIL: matches a full email address (e.g. `user{@literal @}example.com`)
     * - DOMAIN: matches a domain name (e.g. `example.com`)
     * - IP: matches a plain IPv4 address (e.g. `1.2.3.4`) or an IPv4 CIDR block (e.g. `1.2.3.0/24`). Only globally reachable addresses are accepted.
     * - UNKNOWN: deprecated, cannot be used when creating or updating policies, but may be returned for existing entries.
     *   Available values: &#34;EMAIL&#34;, &#34;DOMAIN&#34;, &#34;IP&#34;, &#34;UNKNOWN&#34;.
     * 
     */
    @Import(name="patternType", required=true)
    private Output<String> patternType;

    /**
     * @return Type of pattern matching.
     * - EMAIL: matches a full email address (e.g. `user{@literal @}example.com`)
     * - DOMAIN: matches a domain name (e.g. `example.com`)
     * - IP: matches a plain IPv4 address (e.g. `1.2.3.4`) or an IPv4 CIDR block (e.g. `1.2.3.0/24`). Only globally reachable addresses are accepted.
     * - UNKNOWN: deprecated, cannot be used when creating or updating policies, but may be returned for existing entries.
     *   Available values: &#34;EMAIL&#34;, &#34;DOMAIN&#34;, &#34;IP&#34;, &#34;UNKNOWN&#34;.
     * 
     */
    public Output<String> patternType() {
        return this.patternType;
    }

    private SenderArgs() {}

    private SenderArgs(SenderArgs $) {
        this.accountId = $.accountId;
        this.comments = $.comments;
        this.isRegex = $.isRegex;
        this.pattern = $.pattern;
        this.patternType = $.patternType;
    }

    public static Builder builder() {
        return new Builder();
    }
    public static Builder builder(SenderArgs defaults) {
        return new Builder(defaults);
    }

    public static final class Builder {
        private SenderArgs $;

        public Builder() {
            $ = new SenderArgs();
        }

        public Builder(SenderArgs defaults) {
            $ = new SenderArgs(Objects.requireNonNull(defaults));
        }

        /**
         * @param accountId Identifier.
         * 
         * @return builder
         * 
         */
        public Builder accountId(Output<String> accountId) {
            $.accountId = accountId;
            return this;
        }

        /**
         * @param accountId Identifier.
         * 
         * @return builder
         * 
         */
        public Builder accountId(String accountId) {
            return accountId(Output.of(accountId));
        }

        public Builder comments(@Nullable Output<String> comments) {
            $.comments = comments;
            return this;
        }

        public Builder comments(String comments) {
            return comments(Output.of(comments));
        }

        public Builder isRegex(Output<Boolean> isRegex) {
            $.isRegex = isRegex;
            return this;
        }

        public Builder isRegex(Boolean isRegex) {
            return isRegex(Output.of(isRegex));
        }

        /**
         * @param pattern The pattern value to match against. Format depends on `patternType`:
         * - EMAIL: a valid email address, e.g. `user{@literal @}example.com`
         * - DOMAIN: a valid domain name, e.g. `example.com`
         * - IP: a plain IPv4 address (e.g. `1.2.3.4`) or an IPv4 CIDR block (e.g. `1.2.3.0/24`). Only globally reachable addresses are accepted; private, loopback, link-local, and unspecified addresses are rejected.
         * 
         * @return builder
         * 
         */
        public Builder pattern(Output<String> pattern) {
            $.pattern = pattern;
            return this;
        }

        /**
         * @param pattern The pattern value to match against. Format depends on `patternType`:
         * - EMAIL: a valid email address, e.g. `user{@literal @}example.com`
         * - DOMAIN: a valid domain name, e.g. `example.com`
         * - IP: a plain IPv4 address (e.g. `1.2.3.4`) or an IPv4 CIDR block (e.g. `1.2.3.0/24`). Only globally reachable addresses are accepted; private, loopback, link-local, and unspecified addresses are rejected.
         * 
         * @return builder
         * 
         */
        public Builder pattern(String pattern) {
            return pattern(Output.of(pattern));
        }

        /**
         * @param patternType Type of pattern matching.
         * - EMAIL: matches a full email address (e.g. `user{@literal @}example.com`)
         * - DOMAIN: matches a domain name (e.g. `example.com`)
         * - IP: matches a plain IPv4 address (e.g. `1.2.3.4`) or an IPv4 CIDR block (e.g. `1.2.3.0/24`). Only globally reachable addresses are accepted.
         * - UNKNOWN: deprecated, cannot be used when creating or updating policies, but may be returned for existing entries.
         *   Available values: &#34;EMAIL&#34;, &#34;DOMAIN&#34;, &#34;IP&#34;, &#34;UNKNOWN&#34;.
         * 
         * @return builder
         * 
         */
        public Builder patternType(Output<String> patternType) {
            $.patternType = patternType;
            return this;
        }

        /**
         * @param patternType Type of pattern matching.
         * - EMAIL: matches a full email address (e.g. `user{@literal @}example.com`)
         * - DOMAIN: matches a domain name (e.g. `example.com`)
         * - IP: matches a plain IPv4 address (e.g. `1.2.3.4`) or an IPv4 CIDR block (e.g. `1.2.3.0/24`). Only globally reachable addresses are accepted.
         * - UNKNOWN: deprecated, cannot be used when creating or updating policies, but may be returned for existing entries.
         *   Available values: &#34;EMAIL&#34;, &#34;DOMAIN&#34;, &#34;IP&#34;, &#34;UNKNOWN&#34;.
         * 
         * @return builder
         * 
         */
        public Builder patternType(String patternType) {
            return patternType(Output.of(patternType));
        }

        public SenderArgs build() {
            if ($.accountId == null) {
                throw new MissingRequiredPropertyException("SenderArgs", "accountId");
            }
            if ($.isRegex == null) {
                throw new MissingRequiredPropertyException("SenderArgs", "isRegex");
            }
            if ($.pattern == null) {
                throw new MissingRequiredPropertyException("SenderArgs", "pattern");
            }
            if ($.patternType == null) {
                throw new MissingRequiredPropertyException("SenderArgs", "patternType");
            }
            return $;
        }
    }

}
