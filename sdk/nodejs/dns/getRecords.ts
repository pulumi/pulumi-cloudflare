// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Accepted Permissions
 *
 * - `DNS Read`
 * - `DNS Write`
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as cloudflare from "@pulumi/cloudflare";
 *
 * const exampleDnsRecords = cloudflare.dns.getRecords({
 *     zoneId: "023e105f4ecef8ad9ca31a8372d0c353",
 *     comment: {
 *         absent: "absent",
 *         contains: "ello, worl",
 *         endswith: "o, world",
 *         exact: "Hello, world",
 *         present: "present",
 *         startswith: "Hello, w",
 *     },
 *     content: {
 *         contains: "7.0.0.",
 *         endswith: ".0.1",
 *         exact: "127.0.0.1",
 *         startswith: "127.0.",
 *     },
 *     name: {
 *         contains: "w.example.",
 *         endswith: ".example.com",
 *         exact: "www.example.com",
 *         startswith: "www.example",
 *     },
 *     search: "www.cloudflare.com",
 *     shadowedByName: "sub.example.com",
 *     shadowingName: "www.sub.example.com",
 *     tag: {
 *         absent: "important",
 *         contains: "greeting:ello, worl",
 *         endswith: "greeting:o, world",
 *         exact: "greeting:Hello, world",
 *         present: "important",
 *         startswith: "greeting:Hello, w",
 *     },
 *     type: "A",
 * });
 * ```
 */
export function getRecords(args?: GetRecordsArgs, opts?: pulumi.InvokeOptions): Promise<GetRecordsResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("cloudflare:dns/records:getRecords", {
        "comment": args.comment,
        "content": args.content,
        "direction": args.direction,
        "includeShadowMetadata": args.includeShadowMetadata,
        "match": args.match,
        "maxItems": args.maxItems,
        "name": args.name,
        "order": args.order,
        "proxied": args.proxied,
        "search": args.search,
        "shadowedByName": args.shadowedByName,
        "shadowingName": args.shadowingName,
        "tag": args.tag,
        "tagMatch": args.tagMatch,
        "type": args.type,
        "zoneId": args.zoneId,
    }, opts);
}

/**
 * A collection of arguments for invoking getRecords.
 */
export interface GetRecordsArgs {
    comment?: inputs.dns.GetRecordsComment;
    content?: inputs.dns.GetRecordsContent;
    /**
     * Direction to order DNS records in.
     * Available values: "asc", "desc".
     */
    direction?: string;
    /**
     * Whether to include shadow metadata in the `meta` field of each record in the response. See [Shadowed records](https://developers.cloudflare.com/dns/manage-dns-records/reference/shadowed-records).
     */
    includeShadowMetadata?: boolean;
    /**
     * Whether to match all search requirements or at least one (any). If set to `all`, acts like a logical AND between filters. If set to `any`, acts like a logical OR instead. Note that the interaction between tag filters is controlled by the `tag-match` parameter instead.
     * Available values: "any", "all".
     */
    match?: string;
    /**
     * Max items to fetch, default: 1000
     */
    maxItems?: number;
    name?: inputs.dns.GetRecordsName;
    /**
     * Field to order DNS records by.
     * Available values: "type", "name", "content", "ttl", "proxied".
     */
    order?: string;
    /**
     * Whether the record is receiving the performance and security benefits of Cloudflare.
     */
    proxied?: boolean;
    /**
     * Allows searching in multiple properties of a DNS record simultaneously. This parameter is intended for human users, not automation. Its exact behavior is intentionally left unspecified and is subject to change in the future. This parameter works independently of the `match` setting. For automated searches, please use the other available parameters.
     */
    search?: string;
    /**
     * Filters to records at or below the given NS delegation name, excluding the NS records that form the delegation itself. The value must be a subdomain of the zone; the zone apex is not accepted. Requires `include_shadow_metadata=true`. See [Shadowed records](https://developers.cloudflare.com/dns/manage-dns-records/reference/shadowed-records).
     */
    shadowedByName?: string;
    /**
     * Returns NS records that shadow the given name, searching at the name itself and each of its ancestor names within the zone, excluding the zone apex. The value must be a subdomain of the zone; the zone apex is not accepted. See [Shadowed records](https://developers.cloudflare.com/dns/manage-dns-records/reference/shadowed-records).
     */
    shadowingName?: string;
    tag?: inputs.dns.GetRecordsTag;
    /**
     * Whether to match all tag search requirements or at least one (any). If set to `all`, acts like a logical AND between tag filters. If set to `any`, acts like a logical OR instead. Note that the regular `match` parameter is still used to combine the resulting condition with other filters that aren't related to tags.
     * Available values: "any", "all".
     */
    tagMatch?: string;
    /**
     * Record type.
     * Available values: "A", "AAAA", "CAA", "CERT", "CNAME", "DNSKEY", "DS", "HTTPS", "LOC", "MX", "NAPTR", "NS", "OPENPGPKEY", "PTR", "SMIMEA", "SRV", "SSHFP", "SVCB", "TLSA", "TXT", "URI".
     */
    type?: string;
    /**
     * Identifier.
     */
    zoneId?: string;
}

/**
 * A collection of values returned by getRecords.
 */
export interface GetRecordsResult {
    readonly comment?: outputs.dns.GetRecordsComment;
    readonly content?: outputs.dns.GetRecordsContent;
    /**
     * Direction to order DNS records in.
     * Available values: "asc", "desc".
     */
    readonly direction: string;
    /**
     * Whether to include shadow metadata in the `meta` field of each record in the response. See [Shadowed records](https://developers.cloudflare.com/dns/manage-dns-records/reference/shadowed-records).
     */
    readonly includeShadowMetadata: boolean;
    /**
     * Whether to match all search requirements or at least one (any). If set to `all`, acts like a logical AND between filters. If set to `any`, acts like a logical OR instead. Note that the interaction between tag filters is controlled by the `tag-match` parameter instead.
     * Available values: "any", "all".
     */
    readonly match: string;
    /**
     * Max items to fetch, default: 1000
     */
    readonly maxItems?: number;
    readonly name?: outputs.dns.GetRecordsName;
    /**
     * Field to order DNS records by.
     * Available values: "type", "name", "content", "ttl", "proxied".
     */
    readonly order: string;
    /**
     * Whether the record is receiving the performance and security benefits of Cloudflare.
     */
    readonly proxied: boolean;
    /**
     * The items returned by the data source
     */
    readonly results: outputs.dns.GetRecordsResult[];
    /**
     * Allows searching in multiple properties of a DNS record simultaneously. This parameter is intended for human users, not automation. Its exact behavior is intentionally left unspecified and is subject to change in the future. This parameter works independently of the `match` setting. For automated searches, please use the other available parameters.
     */
    readonly search?: string;
    /**
     * Filters to records at or below the given NS delegation name, excluding the NS records that form the delegation itself. The value must be a subdomain of the zone; the zone apex is not accepted. Requires `include_shadow_metadata=true`. See [Shadowed records](https://developers.cloudflare.com/dns/manage-dns-records/reference/shadowed-records).
     */
    readonly shadowedByName?: string;
    /**
     * Returns NS records that shadow the given name, searching at the name itself and each of its ancestor names within the zone, excluding the zone apex. The value must be a subdomain of the zone; the zone apex is not accepted. See [Shadowed records](https://developers.cloudflare.com/dns/manage-dns-records/reference/shadowed-records).
     */
    readonly shadowingName?: string;
    readonly tag?: outputs.dns.GetRecordsTag;
    /**
     * Whether to match all tag search requirements or at least one (any). If set to `all`, acts like a logical AND between tag filters. If set to `any`, acts like a logical OR instead. Note that the regular `match` parameter is still used to combine the resulting condition with other filters that aren't related to tags.
     * Available values: "any", "all".
     */
    readonly tagMatch: string;
    /**
     * Record type.
     * Available values: "A", "AAAA", "CAA", "CERT", "CNAME", "DNSKEY", "DS", "HTTPS", "LOC", "MX", "NAPTR", "NS", "OPENPGPKEY", "PTR", "SMIMEA", "SRV", "SSHFP", "SVCB", "TLSA", "TXT", "URI".
     */
    readonly type?: string;
    /**
     * Identifier.
     */
    readonly zoneId?: string;
}
/**
 * Accepted Permissions
 *
 * - `DNS Read`
 * - `DNS Write`
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as cloudflare from "@pulumi/cloudflare";
 *
 * const exampleDnsRecords = cloudflare.dns.getRecords({
 *     zoneId: "023e105f4ecef8ad9ca31a8372d0c353",
 *     comment: {
 *         absent: "absent",
 *         contains: "ello, worl",
 *         endswith: "o, world",
 *         exact: "Hello, world",
 *         present: "present",
 *         startswith: "Hello, w",
 *     },
 *     content: {
 *         contains: "7.0.0.",
 *         endswith: ".0.1",
 *         exact: "127.0.0.1",
 *         startswith: "127.0.",
 *     },
 *     name: {
 *         contains: "w.example.",
 *         endswith: ".example.com",
 *         exact: "www.example.com",
 *         startswith: "www.example",
 *     },
 *     search: "www.cloudflare.com",
 *     shadowedByName: "sub.example.com",
 *     shadowingName: "www.sub.example.com",
 *     tag: {
 *         absent: "important",
 *         contains: "greeting:ello, worl",
 *         endswith: "greeting:o, world",
 *         exact: "greeting:Hello, world",
 *         present: "important",
 *         startswith: "greeting:Hello, w",
 *     },
 *     type: "A",
 * });
 * ```
 */
export function getRecordsOutput(args?: GetRecordsOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetRecordsResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("cloudflare:dns/records:getRecords", {
        "comment": args.comment,
        "content": args.content,
        "direction": args.direction,
        "includeShadowMetadata": args.includeShadowMetadata,
        "match": args.match,
        "maxItems": args.maxItems,
        "name": args.name,
        "order": args.order,
        "proxied": args.proxied,
        "search": args.search,
        "shadowedByName": args.shadowedByName,
        "shadowingName": args.shadowingName,
        "tag": args.tag,
        "tagMatch": args.tagMatch,
        "type": args.type,
        "zoneId": args.zoneId,
    }, opts);
}

/**
 * A collection of arguments for invoking getRecords.
 */
export interface GetRecordsOutputArgs {
    comment?: pulumi.Input<inputs.dns.GetRecordsCommentArgs | undefined>;
    content?: pulumi.Input<inputs.dns.GetRecordsContentArgs | undefined>;
    /**
     * Direction to order DNS records in.
     * Available values: "asc", "desc".
     */
    direction?: pulumi.Input<string | undefined>;
    /**
     * Whether to include shadow metadata in the `meta` field of each record in the response. See [Shadowed records](https://developers.cloudflare.com/dns/manage-dns-records/reference/shadowed-records).
     */
    includeShadowMetadata?: pulumi.Input<boolean | undefined>;
    /**
     * Whether to match all search requirements or at least one (any). If set to `all`, acts like a logical AND between filters. If set to `any`, acts like a logical OR instead. Note that the interaction between tag filters is controlled by the `tag-match` parameter instead.
     * Available values: "any", "all".
     */
    match?: pulumi.Input<string | undefined>;
    /**
     * Max items to fetch, default: 1000
     */
    maxItems?: pulumi.Input<number | undefined>;
    name?: pulumi.Input<inputs.dns.GetRecordsNameArgs | undefined>;
    /**
     * Field to order DNS records by.
     * Available values: "type", "name", "content", "ttl", "proxied".
     */
    order?: pulumi.Input<string | undefined>;
    /**
     * Whether the record is receiving the performance and security benefits of Cloudflare.
     */
    proxied?: pulumi.Input<boolean | undefined>;
    /**
     * Allows searching in multiple properties of a DNS record simultaneously. This parameter is intended for human users, not automation. Its exact behavior is intentionally left unspecified and is subject to change in the future. This parameter works independently of the `match` setting. For automated searches, please use the other available parameters.
     */
    search?: pulumi.Input<string | undefined>;
    /**
     * Filters to records at or below the given NS delegation name, excluding the NS records that form the delegation itself. The value must be a subdomain of the zone; the zone apex is not accepted. Requires `include_shadow_metadata=true`. See [Shadowed records](https://developers.cloudflare.com/dns/manage-dns-records/reference/shadowed-records).
     */
    shadowedByName?: pulumi.Input<string | undefined>;
    /**
     * Returns NS records that shadow the given name, searching at the name itself and each of its ancestor names within the zone, excluding the zone apex. The value must be a subdomain of the zone; the zone apex is not accepted. See [Shadowed records](https://developers.cloudflare.com/dns/manage-dns-records/reference/shadowed-records).
     */
    shadowingName?: pulumi.Input<string | undefined>;
    tag?: pulumi.Input<inputs.dns.GetRecordsTagArgs | undefined>;
    /**
     * Whether to match all tag search requirements or at least one (any). If set to `all`, acts like a logical AND between tag filters. If set to `any`, acts like a logical OR instead. Note that the regular `match` parameter is still used to combine the resulting condition with other filters that aren't related to tags.
     * Available values: "any", "all".
     */
    tagMatch?: pulumi.Input<string | undefined>;
    /**
     * Record type.
     * Available values: "A", "AAAA", "CAA", "CERT", "CNAME", "DNSKEY", "DS", "HTTPS", "LOC", "MX", "NAPTR", "NS", "OPENPGPKEY", "PTR", "SMIMEA", "SRV", "SSHFP", "SVCB", "TLSA", "TXT", "URI".
     */
    type?: pulumi.Input<string | undefined>;
    /**
     * Identifier.
     */
    zoneId?: pulumi.Input<string | undefined>;
}
