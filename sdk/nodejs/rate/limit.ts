// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Accepted Permissions
 *
 * - `Firewall Services Read`
 * - `Firewall Services Write`
 *
 * > `cloudflare.rate.Limit` is in a deprecation phase until June 15th, 2025.
 *   During this time period, this resource is still
 *   fully supported but you are strongly advised to move to the
 *   `cloudflare.Ruleset` resource. Full details can be found in the
 *   developer documentation.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as cloudflare from "@pulumi/cloudflare";
 *
 * const exampleRateLimit = new cloudflare.rate.Limit("example_rate_limit", {
 *     zoneId: "023e105f4ecef8ad9ca31a8372d0c353",
 *     action: {
 *         mode: "challenge",
 *         response: {
 *             body: "<error>This request has been rate-limited.</error>",
 *             contentType: "text/xml",
 *         },
 *         timeout: 86400,
 *     },
 *     match: {
 *         headers: [{
 *             name: "Cf-Cache-Status",
 *             op: "ne",
 *             value: "HIT",
 *         }],
 *         request: {
 *             methods: [
 *                 "GET",
 *                 "POST",
 *             ],
 *             schemes: [
 *                 "HTTP",
 *                 "HTTPS",
 *             ],
 *             url: "*.example.org/path*",
 *         },
 *         response: {
 *             originTraffic: true,
 *         },
 *     },
 *     period: 900,
 *     threshold: 60,
 * });
 * ```
 *
 * ## Import
 *
 * ```sh
 * $ pulumi import cloudflare:rate/limit:Limit example '<zone_id>/<rate_limit_id>'
 * ```
 */
export class Limit extends pulumi.CustomResource {
    /**
     * Get an existing Limit resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: LimitState, opts?: pulumi.CustomResourceOptions): Limit {
        return new Limit(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'cloudflare:rate/limit:Limit';

    /**
     * Returns true if the given object is an instance of Limit.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is Limit {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === Limit.__pulumiType;
    }

    /**
     * The action to perform when the threshold of matched traffic within the configured period is exceeded.
     */
    declare public readonly action: pulumi.Output<outputs.rate.LimitAction>;
    /**
     * Criteria specifying when the current rate limit should be bypassed. You can specify that the rate limit should not apply to one or more URLs.
     */
    declare public /*out*/ readonly bypasses: pulumi.Output<outputs.rate.LimitBypass[]>;
    /**
     * An informative summary of the rule. This value is sanitized and any tags will be removed.
     */
    declare public /*out*/ readonly description: pulumi.Output<string>;
    /**
     * When true, indicates that the rate limit is currently disabled.
     */
    declare public /*out*/ readonly disabled: pulumi.Output<boolean>;
    /**
     * Determines which traffic the rate limit counts towards the threshold.
     */
    declare public readonly match: pulumi.Output<outputs.rate.LimitMatch>;
    /**
     * The time in seconds (an integer value) to count matching traffic. If the count exceeds the configured threshold within this period, Cloudflare will perform the configured action.
     */
    declare public readonly period: pulumi.Output<number>;
    /**
     * The threshold that will trigger the configured mitigation action. Configure this value along with the `period` property to establish a threshold per period.
     */
    declare public readonly threshold: pulumi.Output<number>;
    /**
     * Defines an identifier.
     */
    declare public readonly zoneId: pulumi.Output<string>;

    /**
     * Create a Limit resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: LimitArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: LimitArgs | LimitState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as LimitState | undefined;
            resourceInputs["action"] = state?.action;
            resourceInputs["bypasses"] = state?.bypasses;
            resourceInputs["description"] = state?.description;
            resourceInputs["disabled"] = state?.disabled;
            resourceInputs["match"] = state?.match;
            resourceInputs["period"] = state?.period;
            resourceInputs["threshold"] = state?.threshold;
            resourceInputs["zoneId"] = state?.zoneId;
        } else {
            const args = argsOrState as LimitArgs | undefined;
            if (args?.action === undefined && !opts.urn) {
                throw new Error("Missing required property 'action'");
            }
            if (args?.match === undefined && !opts.urn) {
                throw new Error("Missing required property 'match'");
            }
            if (args?.period === undefined && !opts.urn) {
                throw new Error("Missing required property 'period'");
            }
            if (args?.threshold === undefined && !opts.urn) {
                throw new Error("Missing required property 'threshold'");
            }
            if (args?.zoneId === undefined && !opts.urn) {
                throw new Error("Missing required property 'zoneId'");
            }
            resourceInputs["action"] = args?.action;
            resourceInputs["match"] = args?.match;
            resourceInputs["period"] = args?.period;
            resourceInputs["threshold"] = args?.threshold;
            resourceInputs["zoneId"] = args?.zoneId;
            resourceInputs["bypasses"] = undefined /*out*/;
            resourceInputs["description"] = undefined /*out*/;
            resourceInputs["disabled"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        const aliasOpts = { aliases: [{ type: "cloudflare:index/rateLimit:RateLimit" }] };
        opts = pulumi.mergeOptions(opts, aliasOpts);
        super(Limit.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering Limit resources.
 */
export interface LimitState {
    /**
     * The action to perform when the threshold of matched traffic within the configured period is exceeded.
     */
    action?: pulumi.Input<inputs.rate.LimitAction | undefined>;
    /**
     * Criteria specifying when the current rate limit should be bypassed. You can specify that the rate limit should not apply to one or more URLs.
     */
    bypasses?: pulumi.Input<pulumi.Input<inputs.rate.LimitBypass>[] | undefined>;
    /**
     * An informative summary of the rule. This value is sanitized and any tags will be removed.
     */
    description?: pulumi.Input<string | undefined>;
    /**
     * When true, indicates that the rate limit is currently disabled.
     */
    disabled?: pulumi.Input<boolean | undefined>;
    /**
     * Determines which traffic the rate limit counts towards the threshold.
     */
    match?: pulumi.Input<inputs.rate.LimitMatch | undefined>;
    /**
     * The time in seconds (an integer value) to count matching traffic. If the count exceeds the configured threshold within this period, Cloudflare will perform the configured action.
     */
    period?: pulumi.Input<number | undefined>;
    /**
     * The threshold that will trigger the configured mitigation action. Configure this value along with the `period` property to establish a threshold per period.
     */
    threshold?: pulumi.Input<number | undefined>;
    /**
     * Defines an identifier.
     */
    zoneId?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a Limit resource.
 */
export interface LimitArgs {
    /**
     * The action to perform when the threshold of matched traffic within the configured period is exceeded.
     */
    action: pulumi.Input<inputs.rate.LimitAction>;
    /**
     * Determines which traffic the rate limit counts towards the threshold.
     */
    match: pulumi.Input<inputs.rate.LimitMatch>;
    /**
     * The time in seconds (an integer value) to count matching traffic. If the count exceeds the configured threshold within this period, Cloudflare will perform the configured action.
     */
    period: pulumi.Input<number>;
    /**
     * The threshold that will trigger the configured mitigation action. Configure this value along with the `period` property to establish a threshold per period.
     */
    threshold: pulumi.Input<number>;
    /**
     * Defines an identifier.
     */
    zoneId: pulumi.Input<string>;
}
