// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Accepted Permissions
 *
 * - `Cloud Email Security: Read`
 * - `Cloud Email Security: Write`
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as cloudflare from "@pulumi/cloudflare";
 *
 * const exampleEmailSecurityBlockSender = new cloudflare.emailsecurityblock.Sender("example_email_security_block_sender", {
 *     accountId: "023e105f4ecef8ad9ca31a8372d0c353",
 *     isRegex: false,
 *     pattern: "test@example.com",
 *     patternType: "EMAIL",
 *     comments: "block sender with email test@example.com",
 * });
 * ```
 *
 * ## Import
 *
 * ```sh
 * $ pulumi import cloudflare:emailSecurityBlock/sender:Sender example '<account_id>/<pattern_id>'
 * ```
 */
export class Sender extends pulumi.CustomResource {
    /**
     * Get an existing Sender resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: SenderState, opts?: pulumi.CustomResourceOptions): Sender {
        return new Sender(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'cloudflare:emailSecurityBlock/sender:Sender';

    /**
     * Returns true if the given object is an instance of Sender.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is Sender {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === Sender.__pulumiType;
    }

    /**
     * Identifier.
     */
    declare public readonly accountId: pulumi.Output<string>;
    declare public readonly comments: pulumi.Output<string | undefined>;
    declare public /*out*/ readonly createdAt: pulumi.Output<string>;
    declare public readonly isRegex: pulumi.Output<boolean>;
    /**
     * Deprecated, use `modifiedAt` instead. End of life: November 1, 2026.
     *
     * @deprecated Use `modifiedAt` instead.
     */
    declare public /*out*/ readonly lastModified: pulumi.Output<string>;
    declare public /*out*/ readonly modifiedAt: pulumi.Output<string>;
    /**
     * The pattern value to match against. Format depends on `patternType`:
     * - EMAIL: a valid email address, e.g. `user@example.com`
     * - DOMAIN: a valid domain name, e.g. `example.com`
     * - IP: a plain IPv4 address (e.g. `1.2.3.4`) or an IPv4 CIDR block (e.g. `1.2.3.0/24`). Only globally reachable addresses are accepted; private, loopback, link-local, and unspecified addresses are rejected.
     */
    declare public readonly pattern: pulumi.Output<string>;
    /**
     * Type of pattern matching.
     * - EMAIL: matches a full email address (e.g. `user@example.com`)
     * - DOMAIN: matches a domain name (e.g. `example.com`)
     * - IP: matches a plain IPv4 address (e.g. `1.2.3.4`) or an IPv4 CIDR block (e.g. `1.2.3.0/24`). Only globally reachable addresses are accepted.
     * - UNKNOWN: deprecated, cannot be used when creating or updating policies, but may be returned for existing entries.
     *   Available values: "EMAIL", "DOMAIN", "IP", "UNKNOWN".
     */
    declare public readonly patternType: pulumi.Output<string>;

    /**
     * Create a Sender resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: SenderArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: SenderArgs | SenderState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as SenderState | undefined;
            resourceInputs["accountId"] = state?.accountId;
            resourceInputs["comments"] = state?.comments;
            resourceInputs["createdAt"] = state?.createdAt;
            resourceInputs["isRegex"] = state?.isRegex;
            resourceInputs["lastModified"] = state?.lastModified;
            resourceInputs["modifiedAt"] = state?.modifiedAt;
            resourceInputs["pattern"] = state?.pattern;
            resourceInputs["patternType"] = state?.patternType;
        } else {
            const args = argsOrState as SenderArgs | undefined;
            if (args?.accountId === undefined && !opts.urn) {
                throw new Error("Missing required property 'accountId'");
            }
            if (args?.isRegex === undefined && !opts.urn) {
                throw new Error("Missing required property 'isRegex'");
            }
            if (args?.pattern === undefined && !opts.urn) {
                throw new Error("Missing required property 'pattern'");
            }
            if (args?.patternType === undefined && !opts.urn) {
                throw new Error("Missing required property 'patternType'");
            }
            resourceInputs["accountId"] = args?.accountId;
            resourceInputs["comments"] = args?.comments;
            resourceInputs["isRegex"] = args?.isRegex;
            resourceInputs["pattern"] = args?.pattern;
            resourceInputs["patternType"] = args?.patternType;
            resourceInputs["createdAt"] = undefined /*out*/;
            resourceInputs["lastModified"] = undefined /*out*/;
            resourceInputs["modifiedAt"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        const aliasOpts = { aliases: [{ type: "cloudflare:index/emailSecurityBlockSender:EmailSecurityBlockSender" }] };
        opts = pulumi.mergeOptions(opts, aliasOpts);
        super(Sender.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering Sender resources.
 */
export interface SenderState {
    /**
     * Identifier.
     */
    accountId?: pulumi.Input<string | undefined>;
    comments?: pulumi.Input<string | undefined>;
    createdAt?: pulumi.Input<string | undefined>;
    isRegex?: pulumi.Input<boolean | undefined>;
    /**
     * Deprecated, use `modifiedAt` instead. End of life: November 1, 2026.
     *
     * @deprecated Use `modifiedAt` instead.
     */
    lastModified?: pulumi.Input<string | undefined>;
    modifiedAt?: pulumi.Input<string | undefined>;
    /**
     * The pattern value to match against. Format depends on `patternType`:
     * - EMAIL: a valid email address, e.g. `user@example.com`
     * - DOMAIN: a valid domain name, e.g. `example.com`
     * - IP: a plain IPv4 address (e.g. `1.2.3.4`) or an IPv4 CIDR block (e.g. `1.2.3.0/24`). Only globally reachable addresses are accepted; private, loopback, link-local, and unspecified addresses are rejected.
     */
    pattern?: pulumi.Input<string | undefined>;
    /**
     * Type of pattern matching.
     * - EMAIL: matches a full email address (e.g. `user@example.com`)
     * - DOMAIN: matches a domain name (e.g. `example.com`)
     * - IP: matches a plain IPv4 address (e.g. `1.2.3.4`) or an IPv4 CIDR block (e.g. `1.2.3.0/24`). Only globally reachable addresses are accepted.
     * - UNKNOWN: deprecated, cannot be used when creating or updating policies, but may be returned for existing entries.
     *   Available values: "EMAIL", "DOMAIN", "IP", "UNKNOWN".
     */
    patternType?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a Sender resource.
 */
export interface SenderArgs {
    /**
     * Identifier.
     */
    accountId: pulumi.Input<string>;
    comments?: pulumi.Input<string | undefined>;
    isRegex: pulumi.Input<boolean>;
    /**
     * The pattern value to match against. Format depends on `patternType`:
     * - EMAIL: a valid email address, e.g. `user@example.com`
     * - DOMAIN: a valid domain name, e.g. `example.com`
     * - IP: a plain IPv4 address (e.g. `1.2.3.4`) or an IPv4 CIDR block (e.g. `1.2.3.0/24`). Only globally reachable addresses are accepted; private, loopback, link-local, and unspecified addresses are rejected.
     */
    pattern: pulumi.Input<string>;
    /**
     * Type of pattern matching.
     * - EMAIL: matches a full email address (e.g. `user@example.com`)
     * - DOMAIN: matches a domain name (e.g. `example.com`)
     * - IP: matches a plain IPv4 address (e.g. `1.2.3.4`) or an IPv4 CIDR block (e.g. `1.2.3.0/24`). Only globally reachable addresses are accepted.
     * - UNKNOWN: deprecated, cannot be used when creating or updating policies, but may be returned for existing entries.
     *   Available values: "EMAIL", "DOMAIN", "IP", "UNKNOWN".
     */
    patternType: pulumi.Input<string>;
}
