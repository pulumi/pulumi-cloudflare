// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Accepted Permissions
 *
 * - `DNS Read`
 * - `DNS Write`
 * - `Zone DNS Settings Read`
 * - `Zone DNS Settings Write`
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as cloudflare from "@pulumi/cloudflare";
 *
 * const exampleZoneDnsSettings = cloudflare.zone.getDnsSettings({
 *     zoneId: "023e105f4ecef8ad9ca31a8372d0c353",
 * });
 * ```
 */
export function getDnsSettings(args?: GetDnsSettingsArgs, opts?: pulumi.InvokeOptions): Promise<GetDnsSettingsResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("cloudflare:zone/dnsSettings:getDnsSettings", {
        "zoneId": args.zoneId,
    }, opts);
}

/**
 * A collection of arguments for invoking getDnsSettings.
 */
export interface GetDnsSettingsArgs {
    /**
     * Identifier.
     */
    zoneId?: string;
}

/**
 * A collection of values returned by getDnsSettings.
 */
export interface GetDnsSettingsResult {
    /**
     * Whether to flatten all CNAME records in the zone. Note that, due to DNS limitations, a CNAME record at the zone apex will always be flattened.
     */
    readonly flattenAllCnames: boolean;
    /**
     * Whether to enable Foundation DNS Advanced Nameservers on the zone.
     */
    readonly foundationDns: boolean;
    /**
     * Settings for this internal zone.
     */
    readonly internalDns: outputs.zone.GetDnsSettingsInternalDns;
    /**
     * Whether to enable multi-provider DNS, which causes Cloudflare to activate the zone even when non-Cloudflare NS records exist, and to respect NS records at the zone apex during outbound zone transfers.
     */
    readonly multiProvider: boolean;
    /**
     * Settings determining the nameservers through which the zone should be available.
     */
    readonly nameservers: outputs.zone.GetDnsSettingsNameservers;
    /**
     * The time to live (TTL) of the zone's nameserver (NS) records.
     */
    readonly nsTtl: number;
    /**
     * Allows a Secondary DNS zone to use (proxied) override records and CNAME flattening at the zone apex.
     */
    readonly secondaryOverrides: boolean;
    /**
     * Components of the zone's SOA record.
     */
    readonly soa: outputs.zone.GetDnsSettingsSoa;
    /**
     * Identifier.
     */
    readonly zoneId?: string;
    /**
     * Whether the zone mode is a regular or CDN/DNS only zone.
     * Available values: "standard", "cdn*only", "dns*only".
     */
    readonly zoneMode: string;
}
/**
 * Accepted Permissions
 *
 * - `DNS Read`
 * - `DNS Write`
 * - `Zone DNS Settings Read`
 * - `Zone DNS Settings Write`
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as cloudflare from "@pulumi/cloudflare";
 *
 * const exampleZoneDnsSettings = cloudflare.zone.getDnsSettings({
 *     zoneId: "023e105f4ecef8ad9ca31a8372d0c353",
 * });
 * ```
 */
export function getDnsSettingsOutput(args?: GetDnsSettingsOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetDnsSettingsResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("cloudflare:zone/dnsSettings:getDnsSettings", {
        "zoneId": args.zoneId,
    }, opts);
}

/**
 * A collection of arguments for invoking getDnsSettings.
 */
export interface GetDnsSettingsOutputArgs {
    /**
     * Identifier.
     */
    zoneId?: pulumi.Input<string | undefined>;
}
